ФИНАЛЬНЫЙ ТОЧНЫЙ PROMPT для Replit/Codex

(убрать белую плашку, сделать full-screen TikTok-карту объявления, как на втором скрине)

Сделать TikTok-ленту объявлений в MiniApp/Web таким образом:

🎯 1. Карточка объявления должна быть как на втором скрине (который я прислал)

Включая:

фото на весь экран

кнопки «назад», «лайк», «чат», «поделиться»

цена

продавец

метки («Свежее», «Фермер»)

описание

кнопка «Смотреть подробнее»

индикатор свайпа внизу

Полностью сохранить UI, как на втором изображении.

🚫 2. УБРАТЬ БЕЛУЮ ПЛАШКУ, которая появляется под фотографией в TikTok-ленте

Белая плашка появляется из-за:

лишних padding/margin

ненужного safeAreaInset снизу

обёртки контейнера с белым background

неправильной высоты карточки

Нужно устранить полностью.

🧱 3. Карточка должна быть Full-Screen (высота = высоте девайса)

Установить высоту карточки:

const { height: deviceHeight } = Dimensions.get('window');

<View style={{
  height: deviceHeight,
  width: "100%",
  backgroundColor: "transparent",
}}>

🧹 4. Удалить ВСЕ возможные источники белого фона

Найти и удалить:

backgroundColor: '#fff'
paddingBottom: 20
marginBottom: 20


и любые похожие стили
в:

TikTokFeedScreen

AdCardTikTok

родительских View

FlatList container

📷 5. Фото должно занимать ВСЮ высоту

Использовать:

<Image
  source={{ uri: ad.photo }}
  style={{
    width: "100%",
    height: "100%",
    resizeMode: "cover"
  }}
/>


Никакого contain, никакой рамки, никакого белого.

🧩 6. FlatList должна быть “TikTok-типовая”

Включить:

snapToInterval={deviceHeight}
decelerationRate="fast"
bounces={false}
contentContainerStyle={{ paddingBottom: 0 }}
showsVerticalScrollIndicator={false}

🧘 7. Убрать нижний safeArea

Если используется:

paddingBottom: insets.bottom


— удалить.
В Telegram Mini App это создаёт ту самую белую плашку.

🧩 8. Контейнер TikTok-ленты
<View style={{
  flex: 1,
  backgroundColor: "black",
  paddingBottom: 0,
  marginBottom: 0,
}}>


И ВСЁ.

🎯 Итоговое поведение должно быть таким:

Карточка объявления выглядит ровно как на втором твоём скрине.

Фото занимает весь экран без разрывов.

Под фото нет белой полосы.

Лента свайпается как TikTok (full-screen snap).

Верхний блок фильтров («Все», «Даром», «Фермер», «Скидки») остаётся.

Карточка растягивается от верхних фильтров до нижнего таббара.

Именно так, как ты хочешь — идеально, красиво, без белой полосы.