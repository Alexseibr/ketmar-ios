Ок, делаю три отдельных «кусочка», чтобы ты мог кидать их в Replit / Codex / дизайнеру как есть.
Пойду по очереди:

---

## 🧠 1) СУПЕР-ПРОМПТ ДЛЯ BACKEND

**Задача: реализовать HomeDynamicEngine + GeoZoneClassifier + API `/api/home/config` + фильтрацию `/api/ads` по зонам.**

```text
Тебе нужно доработать backend KETMAR Market.

Цель: добавить ДИНАМИЧЕСКУЮ ГЛАВНУЮ СТРАНИЦУ, которая зависит от геолокации:
- village (деревня/агрогородок)
- suburb (окраина/частный сектор)
- city_center (центр города, многоэтажки)

Нужно сделать:

1. Создать модуль GeoZoneClassifier
------------------------------------
Файл: /services/GeoZoneClassifier.js

Функции:
- async getZoneByCoords(lat, lng): Promise<"village" | "suburb" | "city_center">

Логика определения зоны (можно постараться, но начать с простого эвристического варианта):
- Использовать уже существующий reverse geocoding (Nominatim / geoIntelligence.js, если есть).
- Если OSM type = "village" или "hamlet" → "village".
- Если это "city" или "town":
  - Попробовать понять тип застройки:
    - Если в радиусе 1 км много частных домов (low-rise, street с "ul.", "pereulok", private) → "suburb".
    - Если плотная застройка, много адресов/домов → "city_center".
- Дополнительно можно использовать накопленную статистику:
  - Если в этом районе много фермерских объявлений и мало сервисов → "village".
  - Если много объявлений "услуги по двору", "покос" → "suburb".
  - Если много клининга, beauty, авторских брендов → "city_center".

На первом шаге допустима упрощённая логика:
- Если city && расстояние до центра города (0,0 выбранного city center) > X км → "suburb".
- Если village/hamlet → "village".
- Иначе → "city_center".

2. Создать HomeDynamicEngine
----------------------------
Файл: /services/HomeDynamicEngine.js

Функции:
- async getHomeConfig(lat, lng, userId?): Promise<HomeConfig>

Тип HomeConfig:
{
  zone: "village" | "suburb" | "city_center",
  blocks: string[]
}

Внутри:
- Вызвать GeoZoneClassifier.getZoneByCoords(lat, lng).
- На основе зоны вернуть список блоков главной страницы.

Набор блоков (строки):
- "darom"               — объявления даром
- "garden_help"         — помощь по огороду/участку
- "machinery_spare"     — запчасти и техника (МТЗ, мотоблоки и т.п.)
- "tractor_services"    — вспашка, культивация
- "farmer_goods"        — фермерские товары (овощи/фрукты/молоко)
- "village_offers"      — прочие деревенские объявления

- "lawn_mowing"         — покос травы
- "cleaning_house"      — уборка дома/коттеджа
- "repair_house"        — мелкий ремонт/мастера
- "snow_cleaning"       — чистка снега
- "local_shops"         — магазины рядом
- "services"            — универсальные услуги

- "author_brands"       — авторские бренды (инста-товары)
- "beauty"              — бьюти-услуги
- "cleaning"            — клининг квартир
- "handmade"            — хендмейд-изделия
- "trending"            — тренды города
- "demand"              — спрос рядом (из уже существующей Analytics/Recommendation системы)
- "seasonal_fairs"      — сезонные ярмарки (НГ, урожай и т.д.)

Конфиг по зонам (стартовый, можно захардкодить в константу):

Для zone="village":
[
  "darom",
  "garden_help",
  "machinery_spare",
  "tractor_services",
  "farmer_goods",
  "village_offers",
  "demand",
  "handmade",
  "seasonal_fairs"
]

Для zone="suburb":
[
  "garden_help",
  "lawn_mowing",
  "cleaning_house",
  "repair_house",
  "snow_cleaning",
  "local_shops",
  "services",
  "author_brands",
  "demand",
  "trending",
  "seasonal_fairs"
]

Для zone="city_center":
[
  "author_brands",
  "beauty",
  "cleaning",
  "local_shops",
  "services",
  "trending",
  "demand",
  "handmade",
  "seasonal_fairs"
]

Пока можно не учитывать userId, но оставить параметр на будущее.

3. API endpoint /api/home/config
--------------------------------
Файл: routes/home.js (или создать, если нет)

Маршрут:
GET /api/home/config?lat=..&lng=..

Поведение:
- Проверить наличие lat, lng.
- Вызвать HomeDynamicEngine.getHomeConfig(lat, lng, userId).
- Вернуть JSON:
  {
    zone: "village" | "suburb" | "city_center",
    blocks: [...]
  }

Подключить этот маршрут в основной router (app.js / index.js).

4. Фильтрация объявлений /api/ads по зоне
-----------------------------------------
В существующем ads.js (или аналогичном):
- Для запросов, где передаётся zone, например:

GET /api/ads?zone=village

Нужно:
- Ограничить категории и типы объявлений по зоне.

Пример (логика можно упростить, главное — разделение):

Если zone="village":
- Исключить:
  - категорию "beauty"
  - категорию "клининг квартир"
  - магазины fashion/одежда
- Оставить:
  - фермерские товары
  - техника/запчасти
  - услуги по огороду
  - даром

Если zone="city_center":
- Исключить:
  - вспашка, тракторные услуги
  - запчасти МТЗ и тяжёлой техники (можно по списку категорий)
- Оставить:
  - авторские бренды
  - beauty
  - клининг
  - магазины

Если zone="suburb":
- Разрешить:
  - сад/огород-помощь
  - покос
  - уборка домов
  - магазины рядом
  - мастера
- Ограничить:
  - heavy фермерское (глубинное)

5. Тестовый режим
-----------------
Сделать возможность передать zone вручную (для отладки):
- Если параметр zone присутствует в query (/api/home/config?zone=village) — не вычислять по координатам, а использовать его напрямую.

То же самое для /api/ads — zone из query переопределяет геозону, если указано.
```

---

## 📱 2) СУПЕР-ПРОМПТ ДЛЯ FRONTEND

**Задача: HomePage.tsx → запрос `/api/home/config` → рендер блоков → отдельные компоненты для блоков.**

```text
Нужно доработать frontend миниаппа KETMAR (React / TS).

Цель: главная страница (HomePage) должна динамически меняться в зависимости от конфигурации, которую отдаёт backend /api/home/config.

1. HomePage.tsx
----------------
Файл: /miniapp/src/pages/HomePage.tsx (или аналогичный корневой для главной).

Нужно:
- При монтировании страницы:
  - Получить текущие координаты пользователя (lat/lng) — либо через уже существующий geo контекст, либо через Navigator API (если уже есть).
  - Вызвать GET /api/home/config?lat=..&lng=..

- Состояние:
  - loading: boolean
  - error: string | null
  - zone: "village" | "suburb" | "city_center" | null
  - blocks: string[]

Пример:

const [config, setConfig] = useState<{ zone: string; blocks: string[] } | null>(null);

useEffect(() => {
  // 1. Получить гео
  // 2. Вызвать /api/home/config
  // 3. setConfig(response.data)
}, []);

Рендер:
- Если loading → skeleton / loader.
- Если error → показать ошибку/кнопку "повторить".
- Если есть config → отрисовать блоки:

{config.blocks.map(block => (
  <RenderBlock key={block} type={block} zone={config.zone} />
))}

2. Компонент RenderBlock
------------------------
Файл: /miniapp/src/components/home/RenderBlock.tsx

Пример:

interface RenderBlockProps {
  type: string;
  zone: "village" | "suburb" | "city_center";
}

export const RenderBlock: React.FC<RenderBlockProps> = ({ type, zone }) => {
  switch (type) {
    case "darom":
      return <DaromBlock zone={zone} />;
    case "garden_help":
      return <GardenHelpBlock zone={zone} />;
    case "machinery_spare":
      return <MachinerySpareBlock zone={zone} />;
    case "tractor_services":
      return <TractorServicesBlock zone={zone} />;
    case "farmer_goods":
      return <FarmerGoodsBlock zone={zone} />;

    case "village_offers":
      return <VillageOffersBlock zone={zone} />;
    case "lawn_mowing":
      return <LawnMowingBlock zone={zone} />;
    case "cleaning_house":
      return <CleaningHouseBlock zone={zone} />;
    case "repair_house":
      return <RepairHouseBlock zone={zone} />;
    case "snow_cleaning":
      return <SnowCleaningBlock zone={zone} />;
    case "local_shops":
      return <LocalShopsBlock zone={zone} />;

    case "services":
      return <ServicesBlock zone={zone} />;
    case "author_brands":
      return <AuthorBrandsBlock zone={zone} />;
    case "beauty":
      return <BeautyBlock zone={zone} />;
    case "cleaning":
      return <CleaningBlock zone={zone} />;
    case "handmade":
      return <HandmadeBlock zone={zone} />;
    case "trending":
      return <TrendingBlock zone={zone} />;
    case "demand":
      return <DemandBlock zone={zone} />;
    case "seasonal_fairs":
      return <SeasonalFairsBlock zone={zone} />;
    default:
      return null;
  }
};

3. Компоненты блоков
---------------------
Каждый блок — это самостоятельный компонент, который:
- делает запрос к соответствующему API (например, /api/ads?type=farmer_goods&zone=village),
- рендерит список карточек (AdCard / ServiceCard),
- учитывает zone (можно использовать для текста или доп. фильтров).

Папка:
- /miniapp/src/components/home/blocks/

Примеры файлов:
- DaromBlock.tsx
- GardenHelpBlock.tsx
- MachinerySpareBlock.tsx
- TractorServicesBlock.tsx
- FarmerGoodsBlock.tsx
- VillageOffersBlock.tsx
- LawnMowingBlock.tsx
- CleaningHouseBlock.tsx
- RepairHouseBlock.tsx
- SnowCleaningBlock.tsx
- LocalShopsBlock.tsx
- ServicesBlock.tsx
- AuthorBrandsBlock.tsx
- BeautyBlock.tsx
- CleaningBlock.tsx
- HandmadeBlock.tsx
- TrendingBlock.tsx
- DemandBlock.tsx
- SeasonalFairsBlock.tsx

4. Взаимодействие с API
------------------------
Каждый блок должен:
- либо использовать общий хук useAds({ category, zone }),
- либо напрямую дергать API.

Пример для фермерских товаров:

GET /api/ads?categoryGroup=farmer&zone=village&limit=10

Для авторских брендов:

GET /api/ads?categoryGroup=author_brand&zone=city_center&limit=10

Для услуг:

GET /api/services?zone=suburb&type=garden_help

5. UI-различия по зоне
-----------------------
На уровне HomePage можно добавить лёгкий визуальный акцент:

- Если zone = "village":
  - более простые фоны
  - крупнее заголовки
  - крупные кнопки

- Если zone = "city_center":
  - немного более "инстаграмный" стиль:
    - карточки с фото
    - больше белого пространства
    - плавные скроллы

Это можно сделать классами:
- className={`home-root zone-${zone}`}

и завести минимальные CSS/Styled-константы.

6. Режим отладки
-----------------
Сделать возможность принудительно задать zone через query-параметр:
- например, если в URL есть ?debugZone=village — не вызывать /home/config, а использовать эту зону и соответствующий заранее описанный массив блоков.

Это поможет тестировать три варианта интерфейса без реального GPS.
```

---

## 🎨 3) UI-МАКЕТЫ ДЛЯ ТРЁХ ЗОН (ТЗ ДЛЯ ДИЗАЙНЕРА / НЕЙРОСЕТИ)

Можешь дать это дизайнеру или в генератор мокапов.

```text
Нужно нарисовать 3 варианта главного экрана мобильного приложения KETMAR Market (одинавый базовый стиль, но разное содержимое и акценты).

Общие требования:
- Экран: мобильный, вертикальный, iPhone / Android.
- Верхняя шапка: логотип KETMAR, индикатор гео («Ваш район: …»).
- Внизу: навигация (Главная, Категории, Избранное, Профиль).
- Стиль: современный, лёгкий неон (фиолетовый+зелёный, как у нашего бренда), но без перегруза.

1) Макет "ДЕРЕВНЯ" (zone=village)
----------------------------------
Цель: максимально простой и полезный экран для сельской местности / агрогородка.

Содержимое сверху вниз:

1. Блок "Даром рядом":
   - Заголовок: «Даром рядом»
   - 2–3 карточки:
     - «Отдам доски»
     - «Отдам кур»
     - «Отдам кирпич»

2. Блок "Помощь по огороду":
   - Заголовок: «Помощь по огороду»
   - Крупные кнопки-иконки:
     - «Вспахать участок»
     - «Помочь с картошкой»
     - «Покосить траву»

3. Блок "Техника и запчасти":
   - Карточки:
     - «Запчасти МТЗ»
     - «Мотоблоки»
   - Иконки тракторов и гайки.

4. Блок "Фермерские товары":
   - Сеткой 2x2:
     - «Картошка»
     - «Молоко»
     - «Яйца»
     - «Ягоды»

5. Блок "Спрос в деревне":
   - Маленький виджет: «Сейчас ищут: трактор, вспашка, дрова».

Стиль:
- Простой, крупные шрифты и кнопки.
- Минимум декоративных элементов.
- Фон: светлый, с лёгкими зелёными акцентами.

2) Макет "ОКРАИНА" (zone=suburb)
--------------------------------
Цель: частный сектор, пригороды, дачи.

Содержимое сверху вниз:

1. Блок "Участок и дом":
   - Заголовок: «Участок и дом»
   - Крупные плитки:
     - «Покосить траву»
     - «Убрать участок»
     - «Чистка снега»
   - Иконки газонокосилки, мётлы, лопаты.

2. Блок "Уборка и клининг":
   - Заголовок: «Уборка дома»
   - Карточки:
     - «Уборка после ремонта»
     - «Генеральная уборка»
   - Фото интерьеров домов.

3. Блок "Мастера рядом":
   - Плитки:
     - «Ремонт ворот»
     - «Электрик»
     - «Сантехник»

4. Блок "Магазины и сервисы":
   - Список иконок:
     - «Стройматериалы»
     - «Инструменты»
     - «Локальный магазин»

5. Блок "Спрос в районе":
   - Виджет: «Сейчас ищут: покос травы, уборка дома, вывоз мусора».

Стиль:
- Чуть более "городской", но всё ещё практичный.
- Крупные карточки, адаптированные под частный дом.
- Акценты: зелёный (участок), синий (сервисы).

3) Макет "ЦЕНТР ГОРОДА" (zone=city_center)
------------------------------------------
Цель: инстаграмный, красивый, ориентирован на услуги, авторские бренды, магазины.

Содержимое сверху вниз:

1. Блок "Авторские бренды":
   - Заголовок: «Авторские бренды рядом»
   - Горизонтальный карусель из карточек:
     - Татьяна: торты
     - Мария: одежда
     - Ольга: сумки
   - Каждая карточка с большим фото и маленькой кнопкой «Instagram».

2. Блок "Beauty и уход":
   - Плитка 2x2:
     - «Маникюр»
     - «Парикмахер»
     - «Брови/ресницы»
     - «Массаж»
   - С мягкими, светлыми фото.

3. Блок "Уборка квартир":
   - Горизонтальный скролл:
     - «Разовая уборка»
     - «Еженедельный клининг»
     - «Уборка после вечеринки»

4. Блок "Магазины и сервисы":
   - Карточки:
     - «Кофейни рядом»
     - «Магазины одежды»
     - «Салоны красоты»

5. Блок "Что популярно в вашем районе":
   - Виджет-тренды:
     - «Торты на заказ»
     - «Маникюр дома»
     - «Клининг студий»
   - Может быть в виде списка с маленькими тренд-иконками.

Стиль:
- Более “глянцевый”, похожий на Instagram Explore.
- Акцент на фото и визуальные карточки.
- Цвета: белый фон, неоновые акценты KETMAR (фиолетовый/лаймовый).
- Мягкие тени, rounded corners.

Общее:
- В каждом из трёх макетов нижнее меню и верхняя шапка одинаковые, меняется только содержимое главного контента.
- Важно визуально показать разницу зон, но сохранить единый брендовый язык.
```

---

Если хочешь, дальше могу собрать всё это в один **PDF/TZ для команды** или дописать промпт под «Авторский бренд» в рамках этих зон.
