Готов.
Ниже — **единый, мощный, инженерный SUPER PROMPT** для Replit / Codex / DevTeam.
Он включает **всю архитектуру “Динамической Главной Страницы KETMAR”**:
деревня → окраина → центр, с разными блоками, категориями, приоритетами и логикой AI/Geo.

Ты можешь вставить это в Replit как ТЗ — и команда сможет сразу начать реализацию.

---

# 🚀 SUPER PROMPT

## **Тема: Реализовать ДИНАМИЧЕСКУЮ ГЛАВНУЮ СТРАНИЦУ KETMAR в зависимости от геолокации**

*(деревня, окраина, центр города — разные блоки, разные категории, разные приоритеты)*

---

# 🔥 **1. Цель фичи**

Сделать **динамическую главную** в миниаппе, которая перестраивается под тип населённого района:

* **village** (деревня / агрогородок / сельская местность)
* **suburb** (окраина города / частный сектор / район с частными домами)
* **city_center** (центр / многоэтажки / высокая плотность)

Главная должна показывать **только нужные категории**, **нужные услуги**, **нужные ярмарки** и **нужный контент** для конкретной зоны.

---

# 🧠 **2. Нужно разработать HomeDynamicEngine (backend)**

Создать модуль:

```
/services/HomeDynamicEngine.js
```

Он должен:

### ✔ Определять тип зоны по GPS:

* плотность населения
* OSM reverse geocode (village/town/city/district)
* радиус плотности объявлений
* количество фермерских товаров вокруг
* количество услуг вокруг
* средний тип жилья (частный сектор → suburb)

### ✔ Возвращать json конфигурации:

```
GET /api/home/config?lat=xxx&lng=yyy
```

Ответ:

```json
{
  "zone": "suburb",
  "blocks": [
    "services",
    "garden_help",
    "lawn_mowing",
    "cleaning",
    "local_shops",
    "author_brands",
    "demand",
    "trending"
  ]
}
```

### ✔ Генерировать блоки **по приоритетам зоны**.

---

# 🧩 **3. Набор всех блоков, которые могут появиться на главной**

Каждый блок — отдельный React-компонент:

```
blocks/
  DaromBlock.tsx
  FarmerBlock.tsx
  ServiceBlock.tsx
  GardenHelpBlock.tsx
  AuthorBrandBlock.tsx
  CleaningBlock.tsx
  LocalShopsBlock.tsx
  BeautyBlock.tsx
  TrendingBlock.tsx
  DemandBlock.tsx
  MachineryBlock.tsx
  ToolsBlock.tsx
  HandmadeBlock.tsx
  SeasonalFairsBlock.tsx
  LawnMowingBlock.tsx
  SnowCleaningBlock.tsx
  VillageOffersBlock.tsx
```

---

# 🧱 **4. Логика главной страницы (frontend)**

Файл:

```
/pages/HomePage.tsx
```

Должен:

1. Сначала сделать запрос:

```
const { zone, blocks } = await api.get("/home/config?lat&lng");
```

2. Затем рендерить:

```
{blocks.map(block => <RenderBlock type={block} />)}
```

Где:

```
RenderBlock.tsx:
switch(type) { 
  case "darom": return <DaromBlock />;
  case "farmer": return <FarmerBlock />;
  ...
}
```

Главная должна **динамически меняться** без обновления приложения.

---

# 🌾 **5. Конфигурации зон**

## 📍 **1) ДЕРЕВНЯ (village)**

Показывать блоки и только в таком приоритете:

```json
[
  "darom",
  "garden_help",
  "machinery_spare",
  "tractor_services",
  "farmer_goods",
  "village_offers",
  "demand",
  "handmade"
]
```

### Для деревни важно:

* Даром (отдать куры, зерно, мебель)
* Вспашка / копка / огород
* Гараж / ремонт техники
* Овощи/фрукты
* Минимум “инстаграмности”

---

## 🏘 **2) ОКРАИНА (suburb)**

Приоритет:

```json
[
  "garden_help",
  "lawn_mowing",
  "cleaning_house",
  "repair_house",
  "snow_cleaning",
  "local_shops",
  "author_brands",
  "demand",
  "trending"
]
```

### Для окраины важно:

* газоны
* покос
* уборка домов
* вывоз мусора
* мастера
* магазины рядом
* авторские бренды (если спрос есть)

---

## 🏙 **3) ЦЕНТР (city_center)**

Приоритет:

```json
[
  "author_brands",
  "beauty",
  "cleaning",
  "local_shops",
  "services",
  "trending",
  "demand",
  "handmade"
]
```

### Для центра важно:

* торты
* beauty
* барберы
* клининг квартир
* авторские бренды
* тренды
* услуги в городе
* магазины

---

# 🏗 **6. Общее правило: фильтрация контента по зоне**

### В деревне:

* НЕ показывать beauty
* НЕ показывать централизованный клининг
* НЕ показывать city-trending
* НЕ показывать авторские бренды «Инстаграмные»
* Показывать фермерские товары
* Показывать технику

### В городе:

* НЕ показывать тракторы
* НЕ показывать запчасти МТЗ
* НЕ показывать огородную помощь
* Показывать авторские бренды
* Показывать beauty
* Показывать клининг

### На окраине:

* микс деревни и города

---

# 🚦 **7. Логика определения зоны**

Создать:

```
/services/GeoZoneClassifier.js
```

Метод:

```
getZone(lat, lng) =>
  if OSM type = village → "village"
  if OSM type = hamlet → "village"
  if много частных домов → "suburb"
  if много многоэтажек → "city_center"
  если плотность объявлений высокая → "city_center"
  если много фермерских → "village"
  если спрос на услуги > спрос на фермерское → city/suburb
```

---

# 📡 **8. Фильтр объявлений по зоне**

В API:

При запросе `/ads` передавать:

```
zone=village | suburb | city_center
```

И backend должен:

### В деревне:

* исключать city-only категории
* исключать beauty
* исключать fashion
* исключать магазины

### В центре:

* исключать фермерские механизация
* исключать услуги по огороду
* исключать вспашку

---

# 📊 **9. Динамическая Главная должна использовать:**

* сезонность
* тренды
* локальный спрос
* роль пользователя (фермер / магазин / авторский бренд)
* AI рекомендации (уже есть RecommendationService)
* районные особенности

---

# 🎨 **10. UI: Оптимизация под каждую зону**

### Village:

* крупные кнопки
* мало декоративных элементов
* простые цвета

### Suburb:

* карточки среднего размера
* много сервисов
* практично

### City Center:

* красивый UI
* фото-карточки
* плавные анимации
* эстетика Instagram

---

# 🎯 **ФИНАЛЬНАЯ ЦЕЛЬ**

После реализации:

* пользователи в деревнях видят “своё”
* пользователи на окраинах — “своё”
* пользователи в центре — “своё”
* CTR увеличивается на **30–60%**
* “главная” становится персональной
* KETMAR становится **локальным, живым, умным маркетплейсом** как TikTok + Яндекс Go

---

# ❗ Replit, важно:

Реализуйте всё **без ломания существующей ленты**,
Главная работает поверх существующего HomePage.

---

Хочешь — сделаю:

* ✔ отдельный супер-промпт для backend
* ✔ отдельный супер-промпт для frontend
* ✔ UI-макеты для трёх зон

Что нужно следующим шагом?
