✅ PROMPT для Replit / Codex: фикс интерфейса карты объявлений (MiniApp / WebApp)

Сделать полную переработку экрана "Карта объявлений" по следующему техническому ТЗ:

🚧 1. Верхняя плашка — ФИКСИРОВАННАЯ (sticky, не двигается)

Элементы, которые должны быть ЛИПНУТЬ сверху и не прокручиваться:

Кнопка «Назад»

Заголовок: Карта объявлений

Строка поиска («Поиск товаров рядом…»)

Радиусы: 300 м / 1 км / 3 км / 5 км / 10 км / 20 км

Кнопка «Моя геолокация»

Стиль: как сейчас.
Поведение: НЕ ДВИГАЕТСЯ, НЕ СКРОЛЛИТСЯ, НЕ ПРЯЧЕТСЯ.

Использовать:
position: fixed; top: 0; z-index: 1000; background: #fff; backdrop-filter: blur(16px)

🚮 2. БЛОК “Рядом с вами / 12 объявлений” — УБРАТЬ ПОЛНОСТЬЮ

Не показывать:

Нижний белый лист с текстом «Рядом с вами»

Количество объявлений

Нижнюю панель перелистывания

🗺 3. Карта — на весь экран снизу до таббара

После фиксированной верхней панели карта занимает всё оставшееся пространство.

Карта должна:

свободно двигаться

свободно масштабироваться жестами

центрироваться на текущей точке (синим пином)

показывать пины объявлений по координатам

Требования:

Google/Яндекс/Leaflet — в зависимости от проекта (тот же провайдер)

Кластеризация желательно, но можно позже

📍 4. Пины объявлений на карте

Каждый пин должен отображать:

Иконку (мелкая карточка объявления)

Возможно цену (микро-лейбл)

Координаты берём из API /ads?lat=&lng=&radius=

Пины грузятся динамически:

при движении карты → триггер обновления

при изменении радиуса → reload

🪪 5. При нажатии на пин — показывать карточку объявления

При клике:

подлетает нижняя карточка (bottom sheet) с информацией:

фото

цена

заголовок

расстояние

кнопка «Подробнее»

Формат: как карточка в ленте, только компактнее.

❌ 6. При нажатии на крестик — карточка закрывается

Карточка должна закрываться анимацией вниз.

⚙️ 7. API для карты (использовать уже существующее)
Загрузка объявлений:
GET /api/ads?lat=…&lng=…&radius=…

Выбор единичного объявления:
GET /api/ads/:id

Геолокация:

использовать существующий код определения позиции.

🎯 8. Итоговый UI-flows
Экран открываем:

сверху фиксированная панель

снизу сразу карта

без списков, без "рядом"

Пользователь двигает карту:

→ триггер обновления объявлений в зоне видимости
→ обновляем пины

Пользователь нажал на пин:

→ выезжает карточка объявления

Пользователь нажал на фоновую карту или крестик:

→ карточка скрывается

📌 9. Что нужно реализовать прямо в коде

Удалить все блоки со списками под картой.

Верхнюю панель вынести в sticky header.

Карта должна работать как основной интерактивный слой.

Нажатие по пину — показать карточку.

Движение карты — обновлять список пинов.