# Fastlane Configuration

Здесь будут файлы для автоматизации.

## Файлы:

- `Fastfile` — lanes для сборки и деплоя
- `Appfile` — Bundle ID и Team ID
- `Matchfile` — конфигурация сертификатов

## Установка:

```bash
gem install fastlane
fastlane init
```
